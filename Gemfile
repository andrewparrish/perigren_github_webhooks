source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in perigren_github_webhooks.gemspec.
gemspec

group :development do
  gem 'sqlite3'
  gem 'rspec'
  gem 'pry', '~> 0.13.1'
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
